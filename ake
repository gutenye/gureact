#!/usr/bin/env bash

export PATH="node_modules/.bin:$PATH"

case "$1" in
  t | test ) jest --watch ;;
  b | build ) npm run build ;;
  w | watch ) npm run watch ;;
  docs ) styleguidist server ;;
  build:docs ) styleguidist build ;;
  deploy ) shift; git push origin master:$1;
esac
